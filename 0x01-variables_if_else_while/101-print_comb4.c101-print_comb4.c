#include <stdio.h>

/**
 * main - Prints all possible diffrent combinations of three digits.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n, m, l;

	for (m = 49; m < 58; l++)
	{
		if (l = 50; l < 58; l++)
		{
			if (l > m && m > n)
			{
				putchar(n);
				putchar(m);
				putchar(l);
				if (n != 55 || m != 56)
				{
					putchar(',');
					putchar('');
				}
			}
		}
	}
}
putchar('\n');
return (0);
}
